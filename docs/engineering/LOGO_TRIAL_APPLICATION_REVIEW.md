# Logo Trial Application Review

Date: 2026-03-26
Owner: PM

## Decision

`DAMIT` 워드마크 + 폴더/문서 절개형 `D` 심볼을 trial logo asset으로 분리해 적용했다.

## What changed

- 새 자산 추가
  - `public/damit-symbol-foldercut.svg`
  - `public/damit-wordmark-foldercut.svg`
  - `public/damit-lockup-foldercut.svg`
- 시범 적용 표면
  - landing
  - start
  - login
  - home
  - account
  - admin
  - confirm
- 공통 스타일
  - `.brand-lockup-image`
  - `.brand-lockup-image.compact`

## PM view

- 기존 한 장짜리 제안 이미지보다 실제 제품 적용성이 높아졌다.
- 작은 화면에서도 `DAMIT`가 직접 읽혀서 브랜드 인식이 더 빨라졌다.
- 이번 라운드는 `trial application`이므로, 최종 확정 전에는 작은 크기 아이콘성과 wordmark 디테일을 한 번 더 볼 필요가 있다.

## Next

- landing/login/home에서 실제 사용 인상 검수
- `/app`, `/ops` 헤더에까지 확장할지 판단
- 최종 채택 시 favicon/app icon용 소형 최적화 별도 진행
